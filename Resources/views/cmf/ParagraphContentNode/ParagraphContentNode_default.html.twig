{% extends '@MMCmfContent/cmf/ContentNode/ContentNode_default.html.twig' %}

{% block cmf_parapgraphContentNode_widget_content %}
    {% block cmf_contentNode_widget_content %}

        {%  if node.headlineType %}<{{ node.headlineType }} data-cmf-field="headline" data-cmf-field-type="string">{% endif %}
        {{ node.headline|raw }}
        {%  if node.headlineType %}</{{ node.headlineType }}>{% endif %}

        {%  if node.imageSource %}<img src="{{ node.imageSource }}" class="image img-responsive" alt="" title="" />{% endif %}

        <div data-cmf-field="text" data-cmf-field-type="WYSIWYG">{{ node.text|raw }}</div>

    {% endblock %}
{% endblock %}